const { widget } = figma;

export const {
  AutoLayout,
  Text,
  Input,
  SVG,
  Line,
  Rectangle,
  Frame,
  Span,
  Fragment,
  useSyncedState,
  useSyncedMap,
  usePropertyMenu,
  useEffect,
  useWidgetId,
  useWidgetNodeId,
  waitForTask,
} = widget;

export default widget;
